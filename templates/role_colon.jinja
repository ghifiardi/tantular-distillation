{% if bos_token %}{{ bos_token }}{% endif %}{% for message in messages %}{{ (message['role'] | capitalize) + ': ' + message['content'] + '\n\n' }}{% endfor %}{% if add_generation_prompt %}Assistant:{% endif %}
