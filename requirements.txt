# Generation side (any machine that talks to a served teacher)
httpx>=0.27
pyyaml>=6.0
# Chat-template rendering: src/generate_normalized.py and
# src/verify_pass_provenance.py import it directly. It was missing here and
# only worked because it happened to be present in local environments.
jinja2>=3.1

# Tests (regression guards on the source-pack generator)
pytest>=8.0

# Serving side (GPU host only — do NOT install on the Mac, no CUDA on Metal)
# vllm>=0.8.0

# Training side (GPU host only)
# unsloth>=2025.1
# trl>=0.14
# peft>=0.14
# transformers>=4.48
# datasets>=3.0
