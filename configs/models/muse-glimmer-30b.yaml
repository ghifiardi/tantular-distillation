# A REAL teacher this repo already uses (configs/teachers/muse-glimmer.yaml).
# Non-Qwen, so its tokenizer differs from the Qwen student: Mode C is expected
# to be refused for the muse-glimmer -> qwen35-9b pair. DRAFT registry entry.
#
# CORRECTED SOURCE. This entry previously named
# meta-models/Muse-Glimmer-30B-assistant, which is NOT this teacher: it is the
# DFlash speculative-decoding drafter, a five-layer companion network whose own
# card calls it "the lightweight 'drafter' model for Muse Glimmer 30B" and whose
# config declares MuseGlimmerAssistantModel with num_hidden_layers: 5 and
# target_layer_ids into a deeper parent. It ships no tokenizer and no chat
# template, because a drafter shares its parent's. The teacher is the parent,
# meta-models/Muse-Glimmer-30B, which publishes tokenizer.json,
# tokenizer_config.json and chat_template.jinja. Both published derivatives
# (RedHatAI FP8, mlx-community 4-bit) declare that parent as their base_model.
#
# This is a SOURCE correction only. Nothing here is verified yet: the revisions
# remain placeholders and digests_verified stays false until the verifier
# measures the parent against a real snapshot.
schema_version: 1

model_id: meta-models/Muse-Glimmer-30B
revision: a4e59da52a7bc87ae7251dd5545c0dd437c44b68
role: teacher
family: muse-glimmer
generation: "1"

modality:
  text: true
  vision: true

params:
  total_b: 30.0
  active_b: null
  vision_b: null

tokenizer:
  model_id: meta-models/Muse-Glimmer-30B
  revision: a4e59da52a7bc87ae7251dd5545c0dd437c44b68
  sha256: f945b361c8e542b5d2cb6737d03537d656aaeb94f5fe80b9ec7c00234260cf76   # differs from the Qwen student

chat_template:
  source: model
  path: null
  sha256: cfc67e5f349f37690dfd31ed1f18bc4442a9dd32fe39a648f993cb4eb3cae678

architecture_profile: null    # teachers are served by URL; no LoRA layout needed

# Serving config for this same checkpoint; see the note in
# configs/models/qwen35-9b-instruct.yaml. Its fp8/int4/gateway repos are
# deliberately different artefacts and are not identity claims; repos.bf16 is.
serving_config: muse-glimmer

capabilities:
  tools: true
  logprobs: true

license:
  identifier: apache-2.0
  output_training_permitted: true
  reviewed_at: 2026-09-15
  recheck_max_age_days: 180
  evidence_sha256: 2cd78b16a73e905c74136a48441b9548a66dd457504629f73f8ef386bab190fb

digests_verified: true
